import React from 'react'
import AllCourse from './AllCourse'

const Section2 = (props) => {
  return (
    <div>
      section2
      <AllCourse courseData={props.courseData} />
    </div>
  )
}

export default Section2
